name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: postgres://localhost:5432/db
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run lint

  typecheck:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: postgres://localhost:5432/db
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run db:generate
      - run: bunx tsc --noEmit

  build:
    runs-on: ubuntu-latest
    needs: [lint, typecheck]
    env:
      DATABASE_URL: postgres://localhost:5432/db
      NEXT_PUBLIC_APP_URI: http://localhost:3000
      OPENAI_API_KEY: build-placeholder
      E2B_API_KEY: build-placeholder
      VIBE_TEMPLATE: tests-default-team-30d4/vibe-nextjs-dev
      TAVILY_API_KEY: build-placeholder
      B2_KEY_ID: build-placeholder
      B2_APPLICATION_KEY: build-placeholder
      B2_BUCKET_NAME: build-placeholder
      B2_REGION: us-east-005
      B2_ENDPOINT: https://s3.us-east-005.backblazeb2.com
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run db:generate
      - run: bun run build
